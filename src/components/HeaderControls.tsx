import React from "react";

const HeaderControls = () => {
  return (
    <div className="flex items-center gap-3">
      {/* Header controls removed - Profile and Cart are now in bottom navigation */}
    </div>
  );
};

export default HeaderControls;


